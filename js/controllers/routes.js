'use strict';

page('/', skillsController.reveal);
page('/projects', projectsController.reveal);
page('/experience', experienceController.reveal);
page('/interests', interestsController.reveal);
page('/profile', profileController.reveal);

page();
